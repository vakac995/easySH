
import js from '@eslint/js';
import typescript from 'typescript-eslint';
import react from 'eslint-plugin-react';
import reactHooks from 'eslint-plugin-react-hooks';
import reactRefresh from 'eslint-plugin-react-refresh';{% if config.frontend.includeHusky %}
import prettier from 'eslint-plugin-prettier';{% endif %}

export default [
  js.configs.recommended,
  ...typescript.configs.recommended,
  {
    plugins: {
      react,
      'react-hooks': reactHooks,
      'react-refresh': reactRefresh{% if config.frontend.includeHusky %},
      prettier{% endif %}
    },
    rules: {
      'react/jsx-uses-react': 'error',
      'react/jsx-uses-vars': 'error',
      'react-hooks/rules-of-hooks': 'error',
      'react-hooks/exhaustive-deps': 'warn',
      'react-refresh/only-export-components': 'warn'{% if config.frontend.includeHusky %},
      'prettier/prettier': 'error'{% endif %}
    }
  }
];
