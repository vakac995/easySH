
{% if config.frontend.includeExamplePages %}
export function Contact() {
  return (
    <div>
      <h2>Contact</h2>
    </div>
  );
}
{% else %}
// Contact page is disabled. Enable "Include Example Pages" in configuration to use this file.
{% endif %}
